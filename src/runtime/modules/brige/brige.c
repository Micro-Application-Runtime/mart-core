#include "runtime/modules/brige/brige.h"

void runtime_add_brige(runtime_t *rt)
{
    // JSContext *qjs_ctx = rt->qjs_ctx;
    // // 注册 console.log 到 global 对象中
    // JSValue brige = JS_NewObject(qjs_ctx);
    // JS_SetPropertyStr(qjs_ctx, brige, "brige", brige);

    // // 释放 global 对象的引用
    // JS_FreeValue(qjs_ctx, global);
}
